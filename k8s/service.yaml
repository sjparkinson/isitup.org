apiVersion: v1
kind: Service
metadata:
  name: isitup-org
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app.kubernetes.io/name: isitup-org
    app.kubernetes.io/part-of: isitup-org